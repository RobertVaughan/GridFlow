// Optional UI polish for Ollama nodes (loaded automatically if present).
(() => {
  console.info("[gridflow-ollama] UI assets loaded");
  // You could add palette badges, node-specific hints, etc. here.
})();
